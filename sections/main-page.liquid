<section class="section">
  <div class="container">
    <header class="page-head">
      <h1 class="t-h1">{{ page.title }}</h1>
    </header>
    <div class="prose page-body">{{ page.content }}</div>
  </div>
</section>

<style>
  .page-head { text-align: center; padding-block: var(--space-6) var(--space-7); }
  .page-body { margin-inline: auto; }
</style>

{% schema %}
{ "name": "Page", "settings": [] }
{% endschema %}
